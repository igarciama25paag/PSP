<Window x:Class="TxatAurreratua.ServerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TxatAurreratua"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="Zerbitzaria" Height="450" Width="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="WhiteSmoke" Grid.RowSpan="2"/>

        <DockPanel Margin="20">
            <TextBlock DockPanel.Dock="Top" Text="Logs"/>
            <ListBox x:Name="logs" Height="80"/>
        </DockPanel>

        <DockPanel Grid.Row="1" Margin="20,0,20,20">
            <TextBlock DockPanel.Dock="Top" Text="Txat"/>
            <ListBox x:Name="txat"/>
        </DockPanel>

        <DockPanel Grid.Column="1" Grid.RowSpan="2" Margin="10,20">
            <StackPanel DockPanel.Dock="Top">
                <Button x:Name="piztuButton" Click="Piztu" Content="Zerbitzaria piztu" Height="60"/>
                <Button x:Name="itzaliButton" Click="Itzali" Content="Zerbitzaria itzali" Height="60" Margin="0,10,0,0" IsEnabled="False"/>
                <TextBlock Text="Bezeroak" Margin="0,10,0,0"/>
            </StackPanel>
            <ListBox ItemsSource="{Binding Bezeroak}"/>
        </DockPanel>
    </Grid>
</Window>
